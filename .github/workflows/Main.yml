name: Deploy Banana OS to GitHub Pages

on:
  push:
    branches:
      - main  # আপনার প্রধান শাখা যদি 'master' হয়, তবে পরিবর্তন করুন

# একই সাথে একাধিক কাজ চালানো এড়াতে
concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow }}-${{ github.ref }}

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    
    # ⚠️ ফিক্স: এই অংশটি GitHub Pages-এ লেখার জন্য বটের পারমিশন নিশ্চিত করে।
    permissions:
      contents: read  # রিপোজিটরি থেকে ফাইল পড়ার জন্য
      pages: write    # gh-pages শাখায় লেখার অনুমতি দেয় (ডিপ্লয় করার জন্য)
      id-token: write # OpenID Connect (OIDC) টোকেন জেনারেট করার অনুমতি দেয়, যা ডিপ্লয়মেন্টের জন্য দরকার।

    steps:
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # আপনার package.json থেকে নেওয়া

      - name: 💾 Install Dependencies
        # 'npm ci' এরর এড়াতে 'npm install' ব্যবহার করা হলো।
        run: npm install

      - name: 🏗️ Build Angular Application
        # বেস পাথ (/Bananaos/) নিশ্চিত করা হলো।
        run: npm run build -- --base-href=/Bananaos/ 
        env:
          NODE_OPTIONS: --max_old_space_size=8192 

      - name: 📁 Configure Pages for SPA
        # SPA রাউটিং ঠিক রাখতে 404.html তৈরি করা হলো।
        run: cp dist/index.html dist/404.html
        
      # ⚠️ গুরুত্বপূর্ণ ফিক্স: এখানে একটি নতুন অ্যাকশন যোগ করতে হবে 
      # যদি আপনার রিপোজিটরি GitHub Actions দিয়ে Pages ডিপ্লয় করতে সেট করা থাকে।
      # আপনি যদি সেটিংসের Source অপশনে "GitHub Actions" নির্বাচন করে থাকেন, 
      # তবে এই অ্যাকশনটি ব্যবহার করুন, যা 'peaceiris' এর চেয়ে বেশি আধুনিক।
      - name: ⚙️ Setup Pages
        uses: actions/configure-pages@v5

      - name: 🚀 Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist' # আপনার angular.json অনুযায়ী আউটপুট ফোল্ডার

      - name: 🌐 Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

