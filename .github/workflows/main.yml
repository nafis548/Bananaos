name: Deploy Banana OS to GitHub Pages

on:
  push:
    branches:
      - main  # আপনার প্রধান শাখা যদি 'master' হয়, তবে পরিবর্তন করুন

# একই সাথে একাধিক কাজ চালানো এড়াতে
concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow }}-${{ github.ref }}

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    
    # ডিপ্লয় করার জন্য প্রয়োজনীয় অনুমতি
    permissions:
      contents: read
      pages: write
      id-token: write

    steps:
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 💾 Install Dependencies
        # 'npm ci' এরর এড়াতে 'npm install' ব্যবহার করা হলো।
        run: npm install

      - name: 🏗️ Build Angular Application (Fixed Base HREF)
        # ⚠️ FIX: এখানে --base-href=/Bananaos/ যোগ করা হলো। 
        # এটি index.html-এর <base href="/"> কে ওভাররাইড করে।
        run: npm run build -- --base-href=/Bananaos/ 
        env:
          NODE_OPTIONS: --max_old_space_size=8192 

      - name: 📁 Configure Pages for SPA
        run: cp dist/index.html dist/404.html
        
      - name: ⚙️ Setup Pages
        uses: actions/configure-pages@v5

      - name: 🚀 Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

      - name: 🌐 Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
