name: Deploy Banana OS to GitHub Pages

on:
  push:
    branches:
      - main  # আপনার প্রধান শাখা যদি 'master' হয়, তবে 'main'-এর বদলে 'master' লিখবেন

# একই সাথে একাধিক কাজ চালানো এড়াতে
concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow }}-${{ github.ref }}

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    
    # ডিপ্লয় করার জন্য প্রয়োজনীয় অনুমতি
    permissions:
      contents: read
      pages: write
      id-token: write

    steps:
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 💾 Install Dependencies (Fixed)
        # ⚠️ ফিক্স: 'npm ci' এরর আসছিল, তাই 'npm install' ব্যবহার করা হলো।
        run: npm install

      - name: 🏗️ Build Angular Application
        # GitHub Pages-এর জন্য সঠিক বেস পাথ '/Bananaos/' সেট করা হলো।
        run: npm run build -- --base-href=/Bananaos/ 
        env:
          # বড় অ্যাঙ্গুলার প্রজেক্টের জন্য মেমরি বাড়ানো
          NODE_OPTIONS: --max_old_space_size=8192 

      - name: 📁 Configure Pages for SPA (Single Page Application)
        # SPA রাউটিং ঠিক রাখতে 404.html তৈরি করা হলো।
        run: cp dist/index.html dist/404.html

      - name: 🚀 Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          # আউটপুট ফোল্ডার
          publish_dir: ./dist
          # Git History পরিষ্কার রাখতে
          force_orphan: true
          cname: 

